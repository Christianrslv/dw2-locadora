import { GrayDirective } from './gray.directive';

describe('GrayDirective', () => {
  it('should create an instance', () => {
    const directive = new GrayDirective();
    expect(directive).toBeTruthy();
  });
});
